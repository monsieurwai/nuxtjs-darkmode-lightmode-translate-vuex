{
  "name": "nuxt-vuex-theme-switcher",
  "version": "1.0.0",
  "description": "nuxtjs + component who can switch between light and dark theme + translate system with json file",
  "author": "mgelle",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^1.0.0",
    "node-sass": "^4.13.1",
    "sass-loader": "^8.0.2"
  }
}
